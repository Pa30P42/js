const listRef = document.querySelector("#categories");
console.log(`В списке ${listRef.children.length} категории`);

const itemRef = document.querySelectorAll(".item");
console.log(itemRef);

const itemTitleRef = document.querySelector("h2");
console.log(itemTitleRef);

const itemListRef = document.querySelector("ul");
console.log(itemListRef);
